// before.js is indented with 2 spaces on purpose

// indention with tab
// select the whole object to see indentation style
const obj1={
  key1:'value1',
  key2:'value2'
}

// demo on formatting blocks wrapped with {}
// functions
const func1 =(str)=>{
  return 'nothing happened'+str;
}

// if-else
if('something happens'){
  return 'yay!'
}else{
  return 'nay...'
}

